# open-core — Current

## Working on

- System plugin refactor (uncommitted): capabilities, activation upsert,
  REST gate, GraphQL plugin fields, `/system/plugin/manifest`.

## Live

- **v1.8.20** — workspace invites, Cloudflare email, console section
  catalog. Engine **v2.4.49**. Live with `engine:v1.12`.

## Do not

- Mint `is_super_admin` from `IsAdmin` alone.
- Restore `PLUGIN_TYPE_PROJECT` as a user-facing install class.

## Last Updated

2026-09-03
